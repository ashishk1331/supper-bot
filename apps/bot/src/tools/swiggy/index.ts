// TODO: register swiggy_* tools (search_restaurants, get_menu, get_dish_details,
// check_availability, apply_coupon, place_order, track_order) — wraps MCP client
export function registerSwiggyTools(): void {
  // TODO
}
